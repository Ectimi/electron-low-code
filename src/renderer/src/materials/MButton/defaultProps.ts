import { IMaterialProps } from '../types';

const defaultProps: IMaterialProps = {
  style: {
    basic: {
      width: 100,
      height: 35,
    },
    font:{
      fontSize:16
    }
  },
};

export default defaultProps;
